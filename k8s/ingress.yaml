apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cyber-kit-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cyber-kit-frontend-service
            port:
              number: 80
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: cyber-kit-backend-service
            port:
              number: 8000
