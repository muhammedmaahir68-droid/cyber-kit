apiVersion: apps/v1
kind: Deployment
metadata:
  name: cyber-kit-frontend
  labels:
    app: cyber-kit-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cyber-kit-frontend
  template:
    metadata:
      labels:
        app: cyber-kit-frontend
    spec:
      containers:
      - name: frontend
        image: cyber-kit-frontend:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
---
apiVersion: v1
kind: Service
metadata:
  name: cyber-kit-frontend-service
spec:
  selector:
    app: cyber-kit-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
